// Frontend-only deployment config
// Set your backend base URL here when frontend and backend are deployed separately.
// Example:
// window.CONVERTER_API_BASE = "https://your-backend.onrender.com";
// For split deploys, set this before production release.
// If backend and frontend are served from the same origin, leave it empty.

window.CONVERTER_API_BASE = window.CONVERTER_API_BASE || "";
